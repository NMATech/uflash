import { MenuFrag } from "../Fragments/MenuFrag";

const Menu = () => {
  return (
    <section className="bg-white w-full h-max flex flex-col justify-center items-center gap-3 p-3">
      <MenuFrag />
    </section>
  );
};

export default Menu;
